interface:
  display_name: "Merge Review"
  short_description: "Evidence-first MR, merge, range, and release review"
  default_prompt: "Use $merge-review to review this change against its spec, repository standards, engineering risks, and exact source evidence."

policy:
  allow_implicit_invocation: true
